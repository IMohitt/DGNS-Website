# Production canonical/schema base URL. The checked-in fallback is configured in
# src/config/siteConfig.ts.
VITE_SITE_URL=https://dgnsadvisors.ae

# Reserved for a future consent-aware analytics integration. Leave blank unless
# the matching integration and consent flow are deliberately enabled.
VITE_GA_ID=
VITE_META_PIXEL_ID=
